name: Deploy

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - uses: actions/checkout@v2
      - name: Publish
        uses: cloudflare/wrangler-action@1.2.0
        with:
          apiToken: 3tGyWAatqFlgmMnn4-feDwUWVAw8yNykE8Lo8QgL
          #${{ secrets.CF_API_TOKEN }}

